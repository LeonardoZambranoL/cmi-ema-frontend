export type FAQType = {
    question: string;
    answer: string;
}

export type CountryType = {
    id: string;
    name: string;
}

export type SchoolType = {
    id: string;
    name: string;
}

export type RessourceType = {
    title: string;
    description: string;
    content: string;
    link: string;
    free: boolean;
}
